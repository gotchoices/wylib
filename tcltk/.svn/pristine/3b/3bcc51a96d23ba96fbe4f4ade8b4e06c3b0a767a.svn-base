#!/usr/bin/tclsh
#include(Copyright)

#if {$::tcl_platform(pointerSize) == 8} {
#    load ./libwylib-x86_64.so
#} else {
#    load ./libwylib.so
#}
#
#do this instead so we get dia loaded
package require wylib

puts [crypwd XX hello]
